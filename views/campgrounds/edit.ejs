<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EDIT</title>
</head>

<body>
    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input value="<%= campground.title %>" type="text" name="campground[title]" id="title"
                placeholder="Title of the camp">
        </div>
        <div>
            <label for="location">Location</label>
            <input value="<%= campground.location %>" type="text" name="campground[location]" id="location"
                placeholder="Where's it at?">
        </div>
        <button>Edit Camp</button>
    </form>
</body>

</html>